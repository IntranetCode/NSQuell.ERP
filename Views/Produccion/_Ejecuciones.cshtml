@model ERP.NSQuell.Models.ProduccionBandejaVm

@{
    Layout = null;

    static string Txt(string? value)
    {
        return string.IsNullOrWhiteSpace(value) ? "-" : value.Trim();
    }

    static string Num(int? value)
    {
        return value.HasValue ? value.Value.ToString("N0") : "-";
    }

    static string Fecha(DateTime? value)
    {
        return value.HasValue ? value.Value.ToString("dd/MM/yyyy HH:mm") : "-";
    }

    static string ClaseEstado(int estatusId)
    {
        if (estatusId == ERP.NSQuell.Models.ProduccionEstatus.EnProduccion)
            return "ejec-row-produccion";

        if (estatusId == ERP.NSQuell.Models.ProduccionEstatus.EnPreparacion)
            return "ejec-row-preparacion";

        if (estatusId == ERP.NSQuell.Models.ProduccionEstatus.Pausado)
            return "ejec-row-pausado";

        if (estatusId == ERP.NSQuell.Models.ProduccionEstatus.Terminado ||
            estatusId == ERP.NSQuell.Models.ProduccionEstatus.TerminadoParcial)
            return "ejec-row-terminado";

        return "ejec-row-default";
    }

    static string ClaseBadgeEstado(int estatusId)
    {
        if (estatusId == ERP.NSQuell.Models.ProduccionEstatus.EnProduccion)
            return "estado-produccion";

        if (estatusId == ERP.NSQuell.Models.ProduccionEstatus.EnPreparacion)
            return "estado-preparacion";

        if (estatusId == ERP.NSQuell.Models.ProduccionEstatus.Pausado)
            return "estado-pausado";

        if (estatusId == ERP.NSQuell.Models.ProduccionEstatus.Terminado ||
            estatusId == ERP.NSQuell.Models.ProduccionEstatus.TerminadoParcial)
            return "estado-terminado";

        return "estado-default";
    }

    static string IconoEstado(int estatusId)
    {
        if (estatusId == ERP.NSQuell.Models.ProduccionEstatus.EnProduccion)
            return "bi-play-circle-fill";

        if (estatusId == ERP.NSQuell.Models.ProduccionEstatus.EnPreparacion)
            return "bi-tools";

        if (estatusId == ERP.NSQuell.Models.ProduccionEstatus.Pausado)
            return "bi-pause-circle-fill";

        if (estatusId == ERP.NSQuell.Models.ProduccionEstatus.Terminado ||
            estatusId == ERP.NSQuell.Models.ProduccionEstatus.TerminadoParcial)
            return "bi-check-circle-fill";

        return "bi-circle-fill";
    }

    var todas = Model.Ejecuciones
        .OrderBy(x => string.IsNullOrWhiteSpace(x.MaquinaCodigo) ? "ZZZ" : x.MaquinaCodigo)
        .ThenBy(x => x.ProgramaProduccionID)
        .ThenBy(x => x.EjecucionProduccionID)
        .ToList();

    var activas = todas
        .Where(x =>
            x.EstatusID == ERP.NSQuell.Models.ProduccionEstatus.Pendiente ||
            x.EstatusID == ERP.NSQuell.Models.ProduccionEstatus.EnPreparacion ||
            x.EstatusID == ERP.NSQuell.Models.ProduccionEstatus.EnProduccion ||
            x.EstatusID == ERP.NSQuell.Models.ProduccionEstatus.Pausado)
        .ToList();

    var mostrarTerminadas =
        Model.EstatusID == ERP.NSQuell.Models.ProduccionEstatus.Terminado ||
        Model.EstatusID == ERP.NSQuell.Models.ProduccionEstatus.TerminadoParcial;

    List<ERP.NSQuell.Models.ProduccionEjecucionVm> visibles;

    if (Model.EstatusID.HasValue && Model.EstatusID.Value > 0)
    {
        if (mostrarTerminadas)
        {
            visibles = todas
                .Where(x =>
                    x.EstatusID == ERP.NSQuell.Models.ProduccionEstatus.Terminado ||
                    x.EstatusID == ERP.NSQuell.Models.ProduccionEstatus.TerminadoParcial)
                .ToList();
        }
        else
        {
            visibles = todas
                .Where(x => x.EstatusID == Model.EstatusID.Value)
                .ToList();
        }
    }
    else
    {
        visibles = activas;
    }

    var totalTerminadas = todas.Count(x =>
        x.EstatusID == ERP.NSQuell.Models.ProduccionEstatus.Terminado ||
        x.EstatusID == ERP.NSQuell.Models.ProduccionEstatus.TerminadoParcial);

    var grupos = visibles
        .GroupBy(x => new
        {
            MaquinaID = x.MaquinaID ?? 0,
            Codigo = string.IsNullOrWhiteSpace(x.MaquinaCodigo)
                ? "SIN MÁQUINA"
                : x.MaquinaCodigo!.Trim(),
            Nombre = string.IsNullOrWhiteSpace(x.MaquinaNombre)
                ? ""
                : x.MaquinaNombre!.Trim()
        })
        .OrderBy(x => x.Key.Codigo)
        .ToList();
}

<style>
    .ejec-panel {
        --ej-blue: #003b82;
        --ej-border: #d7e2f1;
        --ej-muted: #667085;
        --ej-text: #182437;
    }

    .ejec-toolbar {
        border: 1px solid var(--ej-border);
        border-radius: 16px;
        background: #fff;
        padding: 12px 14px;
        margin-bottom: 10px;
    }

    .ejec-toolbar-top {
        display: flex;
        justify-content: space-between;
        align-items: flex-start;
        gap: 10px;
        flex-wrap: wrap;
    }

    .ejec-title {
        margin: 0;
        color: var(--ej-blue);
        font-size: 1rem;
        font-weight: 950;
    }

    .ejec-sub {
        margin-top: 2px;
        color: var(--ej-muted);
        font-size: .75rem;
        font-weight: 650;
    }

    .ejec-tabs {
        display: flex;
        gap: 8px;
        flex-wrap: wrap;
        margin-top: 12px;
    }

    .ejec-tab {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 7px;
        min-height: 36px;
        padding: 7px 12px;
        border: 1px solid #c8d7ea;
        border-radius: 9px;
        background: linear-gradient(180deg, #ffffff 0%, #f8fbff 100%);
        color: #344054;
        text-decoration: none;
        font-size: .74rem;
        font-weight: 900;
        line-height: 1;
        box-shadow: 0 1px 2px rgba(16, 24, 40, .04);
        transition:
            transform .16s ease,
            box-shadow .16s ease,
            border-color .16s ease,
            background .16s ease,
            color .16s ease;
        cursor: pointer;
        user-select: none;
    }

        .ejec-tab > i {
            width: 22px;
            height: 22px;
            display: inline-grid;
            place-items: center;
            flex: 0 0 22px;
            border-radius: 7px;
            background: #eef4fb;
            color: #496785;
            font-size: .72rem;
            transition: background .16s ease, color .16s ease;
        }

        .ejec-tab:hover {
            transform: translateY(-1px);
            border-color: #8fb7ee;
            background: #f7fbff;
            color: var(--ej-blue);
            box-shadow: 0 5px 12px rgba(0, 59, 130, .10);
        }

        .ejec-tab:hover > i {
            background: #dcecff;
            color: var(--ej-blue);
        }

        .ejec-tab:active {
            transform: translateY(0);
            box-shadow: 0 1px 3px rgba(16, 24, 40, .08);
        }

        .ejec-tab:focus-visible {
            outline: 3px solid rgba(37, 99, 235, .16);
            outline-offset: 2px;
        }

        .ejec-tab.is-active {
            border-color: #6fa7e8;
            background: #edf5ff;
            color: var(--ej-blue);
            box-shadow: inset 0 0 0 1px rgba(0, 59, 130, .05), 0 3px 8px rgba(0, 59, 130, .08);
        }

        .ejec-tab.is-active > i {
            background: var(--ej-blue);
            color: #fff;
        }

    .ejec-tabs .ejec-tab:nth-child(2).is-active {
        border-color: #8db8f5;
        background: #edf5ff;
        color: #1d4ed8;
    }

    .ejec-tabs .ejec-tab:nth-child(2).is-active > i {
        background: #2563eb;
        color: #fff;
    }

    .ejec-tabs .ejec-tab:nth-child(3).is-active {
        border-color: #e7bd55;
        background: #fff8e7;
        color: #8a5b08;
    }

    .ejec-tabs .ejec-tab:nth-child(3).is-active > i {
        background: #d99a13;
        color: #fff;
    }

    .ejec-tabs .ejec-tab:nth-child(4).is-active {
        border-color: #e9a0a0;
        background: #fff1f1;
        color: #b42318;
    }

    .ejec-tabs .ejec-tab:nth-child(4).is-active > i {
        background: #d92d20;
        color: #fff;
    }

    .ejec-tabs .ejec-tab:nth-child(5).is-active {
        border-color: #85c997;
        background: #eefaf1;
        color: #166534;
    }

    .ejec-tabs .ejec-tab:nth-child(5).is-active > i {
        background: #16a34a;
        color: #fff;
    }

    .ejec-tab-count {
        min-width: 21px;
        height: 21px;
        padding: 0 6px;
        border-radius: 7px;
        display: inline-grid;
        place-items: center;
        background: #fff;
        border: 1px solid #d3deeb;
        color: #344054;
        font-size: .64rem;
        font-weight: 950;
        line-height: 1;
        box-shadow: 0 1px 2px rgba(16, 24, 40, .04);
    }

        .ejec-tab.is-active .ejec-tab-count {
            border-color: rgba(255, 255, 255, .85);
            background: rgba(255, 255, 255, .92);
            color: currentColor;
        }

    .ejec-visible-count {
        display: inline-flex;
        align-items: center;
        gap: 5px;
        min-height: 31px;
        padding: 6px 10px;
        border: 1px solid #bfd3f2;
        border-radius: 9px;
        background: #f8fbff;
        color: var(--ej-blue);
        font-size: .7rem;
        font-weight: 900;
    }

    .ejec-machine {
        border: 1px solid var(--ej-border);
        border-radius: 14px;
        background: #fff;
        overflow: hidden;
        margin-bottom: 9px;
    }

    .ejec-machine-head {
        min-height: 42px;
        padding: 7px 11px;
        border-bottom: 1px solid var(--ej-border);
        background: #f8fbff;
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 8px;
        flex-wrap: wrap;
    }

    .ejec-machine-code {
        color: var(--ej-blue);
        font-size: .82rem;
        font-weight: 950;
    }

    .ejec-machine-name {
        margin-left: 5px;
        color: var(--ej-muted);
        font-size: .68rem;
        font-weight: 650;
    }

    .ejec-machine-count {
        display: inline-flex;
        align-items: center;
        gap: 4px;
        padding: 4px 7px;
        border: 1px solid #bfd3f2;
        border-radius: 999px;
        background: #fff;
        color: var(--ej-blue);
        font-size: .66rem;
        font-weight: 900;
    }

    .ejec-table-wrap {
        overflow-x: auto;
    }

    .ejec-table {
        width: 100%;
        min-width: 1320px;
        border-collapse: collapse;
        table-layout: fixed;
        font-size: .72rem;
    }

        .ejec-table th {
            padding: 7px 8px;
            border-bottom: 1px solid #d8e2ef;
            background: #fbfcfe;
            color: #667085;
            font-size: .61rem;
            font-weight: 950;
            text-transform: uppercase;
            letter-spacing: .025em;
            white-space: nowrap;
            text-align: left;
        }

        .ejec-table td {
            padding: 7px 8px;
            border-bottom: 1px solid #edf1f6;
            color: #344054;
            vertical-align: middle;
        }

        .ejec-table tbody tr:last-child td {
            border-bottom: 0;
        }

        .ejec-table tbody tr {
            border-left: 4px solid #98a2b3;
        }

            .ejec-table tbody tr:hover {
                background: #f8fbff;
            }

    .ejec-row-produccion {
        border-left-color: #2563eb !important;
        background: #eff6ff;
    }

    .ejec-row-preparacion {
        border-left-color: #f59e0b !important;
        background: #fffbeb;
    }

    .ejec-row-pausado {
        border-left-color: #dc2626 !important;
        background: #fef2f2;
    }

    .ejec-row-terminado {
        border-left-color: #16a34a !important;
        background: #f0fdf4;
    }

    .ejec-row-produccion:hover {
        background: #dbeafe !important;
    }

    .ejec-row-preparacion:hover {
        background: #fef3c7 !important;
    }

    .ejec-row-pausado:hover {
        background: #fee2e2 !important;
    }

    .ejec-row-terminado:hover {
        background: #dcfce7 !important;
    }

    .ejec-programa {
        color: var(--ej-blue);
        font-weight: 950;
        white-space: nowrap;
    }

    .ejec-small {
        display: block;
        margin-top: 1px;
        color: #667085;
        font-size: .65rem;
        font-weight: 650;
        line-height: 1.15;
    }

    .ejec-parte {
        color: #182437;
        font-weight: 850;
        line-height: 1.15;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
    }

    .ejec-desc {
        display: block;
        margin-top: 2px;
        color: #667085;
        font-size: .64rem;
        line-height: 1.15;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
    }

    .ejec-status {
        display: inline-flex;
        align-items: center;
        gap: 4px;
        padding: 4px 7px;
        border: 1px solid #d0d5dd;
        border-radius: 999px;
        background: #fff;
        color: #344054;
        font-size: .65rem;
        font-weight: 900;
        white-space: nowrap;
    }

    .estado-produccion {
        border-color: #93c5fd;
        background: #dbeafe;
        color: #1d4ed8;
        box-shadow: 0 0 0 2px rgba(37, 99, 235, .05);
    }

    .estado-preparacion {
        border-color: #fcd34d;
        background: #fef3c7;
        color: #92400e;
    }

    .estado-pausado {
        border-color: #fca5a5;
        background: #fee2e2;
        color: #b91c1c;
    }

    .estado-terminado {
        border-color: #86efac;
        background: #dcfce7;
        color: #166534;
    }

    .ejec-num {
        font-weight: 900;
        text-align: right;
        white-space: nowrap;
    }

        .ejec-num.ok {
            color: #067647;
        }

        .ejec-num.sos {
            color: #b54708;
        }

        .ejec-num.scrap {
            color: #b42318;
        }

    .ejec-avance {
        min-width: 115px;
    }

    .ejec-avance-top {
        display: flex;
        justify-content: space-between;
        gap: 6px;
        margin-bottom: 4px;
        color: #475467;
        font-size: .65rem;
        font-weight: 850;
    }

    .ejec-progress {
        height: 6px;
        border-radius: 999px;
        overflow: hidden;
        background: #e9eef7;
    }

        .ejec-progress > span {
            display: block;
            height: 100%;
            border-radius: 999px;
            background: #4b8fd8;
        }

    .ejec-row-preparacion .ejec-progress > span {
        background: #e6b422;
    }

    .ejec-row-pausado .ejec-progress > span {
        background: #d46a78;
    }

    .ejec-row-terminado .ejec-progress > span {
        background: #4f9f72;
    }

    .ejec-action {
        text-align: center;
        white-space: nowrap;
    }

        .ejec-action .btn {
            border-radius: 8px;
            padding: 4px 7px;
            font-size: .67rem;
            font-weight: 900;
        }

    .ejec-empty {
        border: 1px solid var(--ej-border);
        border-radius: 16px;
        background: #fff;
        padding: 34px 18px;
        text-align: center;
        color: var(--ej-muted);
        font-weight: 700;
    }

        .ejec-empty i {
            display: block;
            margin-bottom: 6px;
            font-size: 1.65rem;
            color: #98a2b3;
        }

    @@media (max-width: 900px) {
        .ejec-table {
            min-width: 1180px;
        }
    }

    /* =========================================================
       REFINAMIENTO PROFESIONAL - PANEL DE EJECUCIONES
       ========================================================= */

    .ejec-panel {
        border: 1px solid #d8e2ef;
        border-radius: 15px;
        background: #fff;
        overflow: hidden;
        box-shadow: 0 5px 16px rgba(16, 42, 78, .04);
    }

    .ejec-toolbar {
        margin: 0;
        padding: 14px 15px 13px;
        border: 0;
        border-bottom: 1px solid #dfe7f0;
        border-radius: 0;
        background: linear-gradient(180deg, #fbfcfe 0%, #f8fafc 100%);
    }

    .ejec-toolbar-top {
        align-items: center;
    }

    .ejec-title {
        color: #163b68;
        font-size: .98rem;
        font-weight: 850;
        letter-spacing: -.01em;
    }

    .ejec-title i {
        width: 25px;
        height: 25px;
        display: inline-grid;
        place-items: center;
        margin-right: 6px !important;
        border: 1px solid #d5e2f1;
        border-radius: 8px;
        background: #eef5fd;
        color: #2f6fbd;
        font-size: .75rem;
    }

    .ejec-sub {
        margin-top: 4px;
        margin-left: 33px;
        color: #728096;
        font-size: .73rem;
        font-weight: 600;
    }

    .ejec-visible-count {
        min-height: 30px;
        border-color: #d2deeb;
        background: #fff;
        color: #49627f;
        font-size: .67rem;
        box-shadow: 0 1px 2px rgba(16,24,40,.03);
    }

    .ejec-tabs {
        gap: 7px;
        margin-top: 11px;
    }

    .ejec-tab {
        min-height: 35px;
        padding: 6px 10px;
        border-color: #d1dce8;
        border-radius: 8px;
        background: #fff;
        color: #42526a;
        font-size: .71rem;
        font-weight: 800;
        box-shadow: none;
    }

    .ejec-tab > i {
        width: 21px;
        height: 21px;
        flex-basis: 21px;
        border-radius: 6px;
        background: #f0f4f8;
        color: #60758d;
        font-size: .68rem;
    }

    .ejec-tab:hover {
        transform: none;
        border-color: #a9bfda;
        background: #f8fbff;
        box-shadow: 0 2px 7px rgba(16,42,78,.06);
    }

    .ejec-tab.is-active {
        border-color: #7ba8dc;
        background: #eef5fd;
        box-shadow: inset 0 -2px 0 #2f6fbd;
    }

    .ejec-tab-count {
        min-width: 20px;
        height: 20px;
        border-radius: 6px;
        font-size: .61rem;
        box-shadow: none;
    }

    .ejec-empty {
        min-height: 165px;
        padding: 30px 18px;
        border: 0;
        border-radius: 0;
        background: #fff;
        color: #6d7c90;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
    }

    .ejec-empty-icon {
        width: 42px;
        height: 42px;
        display: grid;
        place-items: center;
        margin-bottom: 10px;
        border: 1px solid #d8e2ef;
        border-radius: 12px;
        background: #f7f9fc;
        color: #8392a5;
    }

    .ejec-empty-icon i {
        margin: 0;
        font-size: 1.15rem;
        color: inherit;
    }

    .ejec-empty-title {
        color: #344054;
        font-size: .84rem;
        font-weight: 850;
    }

    .ejec-empty-text {
        max-width: 440px;
        margin-top: 4px;
        color: #7a889b;
        font-size: .72rem;
        font-weight: 600;
        line-height: 1.45;
    }

    .ejec-machine {
        margin: 10px 11px;
        border-radius: 11px;
        box-shadow: none;
    }

    .ejec-machine-head {
        min-height: 43px;
        background: #f8fafc;
    }

    .ejec-machine-code {
        color: #163b68;
    }

    .ejec-machine-count {
        border-color: #d2deeb;
        border-radius: 7px;
        color: #49627f;
    }

    .ejec-table th {
        background: #f7f9fc;
        color: #596a80;
    }

    .ejec-table tbody tr:hover {
        box-shadow: inset 3px 0 0 rgba(47,111,189,.16);
    }

    @@media (max-width: 700px) {
        .ejec-sub {
            margin-left: 0;
        }

        .ejec-toolbar-top {
            align-items: flex-start;
        }
    }



    /* =========================================================
       TEMA GLOBAL ERP
       La vista responde directamente a html[data-nsq-theme].
       ========================================================= */
    html[data-nsq-theme="dark"] .ejec-panel {
        --ej-blue: #8cc6ff;
        --ej-border: #2d3e55;
        --ej-muted: #9aa9bd;
        --ej-text: #e7eef9;
        background: #111b2d;
        border-color: #2d3e55;
        box-shadow: 0 5px 16px rgba(0,0,0,.16);
    }

    html[data-nsq-theme="dark"] .ejec-toolbar {
        background: linear-gradient(180deg,#152238 0%,#111b2d 100%);
        border-bottom-color: #2d3e55;
    }

    html[data-nsq-theme="dark"] .ejec-title,
    html[data-nsq-theme="dark"] .ejec-machine-code,
    html[data-nsq-theme="dark"] .ejec-programa,
    html[data-nsq-theme="dark"] .ejec-parte,
    html[data-nsq-theme="dark"] .ejec-empty-title {
        color: #eaf2ff;
    }

    html[data-nsq-theme="dark"] .ejec-title i {
        background: #102b4c;
        border-color: #28598b;
        color: #9dcaff;
    }

    html[data-nsq-theme="dark"] .ejec-sub,
    html[data-nsq-theme="dark"] .ejec-machine-name,
    html[data-nsq-theme="dark"] .ejec-small,
    html[data-nsq-theme="dark"] .ejec-desc,
    html[data-nsq-theme="dark"] .ejec-empty-text {
        color: #9aa9bd;
    }

    html[data-nsq-theme="dark"] .ejec-visible-count,
    html[data-nsq-theme="dark"] .ejec-tab,
    html[data-nsq-theme="dark"] .ejec-machine-count,
    html[data-nsq-theme="dark"] .ejec-tab-count {
        background: #17263c;
        border-color: #354961;
        color: #cbd7e7;
    }

    html[data-nsq-theme="dark"] .ejec-tab > i {
        background: #223149;
        color: #aebbd0;
    }

    html[data-nsq-theme="dark"] .ejec-tab:hover {
        background: #1a2941;
        border-color: #58779e;
        color: #f3f7ff;
        box-shadow: none;
    }

    html[data-nsq-theme="dark"] .ejec-tab:hover > i {
        background: #27405f;
        color: #d9ebff;
    }

    html[data-nsq-theme="dark"] .ejec-tab.is-active {
        background: #15355a;
        border-color: #5ba6f5;
        color: #d9ebff;
        box-shadow: inset 0 -2px 0 #5ba6f5;
    }

    html[data-nsq-theme="dark"] .ejec-tabs .ejec-tab:nth-child(3).is-active {
        background: #322815;
        border-color: #d39c29;
        color: #ffd66b;
        box-shadow: inset 0 -2px 0 #d39c29;
    }

    html[data-nsq-theme="dark"] .ejec-tabs .ejec-tab:nth-child(4).is-active {
        background: #351719;
        border-color: #a83d46;
        color: #ff9ca8;
        box-shadow: inset 0 -2px 0 #a83d46;
    }

    html[data-nsq-theme="dark"] .ejec-tabs .ejec-tab:nth-child(5).is-active {
        background: #102a20;
        border-color: #286446;
        color: #79e3a8;
        box-shadow: inset 0 -2px 0 #286446;
    }

    html[data-nsq-theme="dark"] .ejec-tab.is-active .ejec-tab-count {
        background: rgba(255,255,255,.08);
        border-color: rgba(255,255,255,.18);
        color: inherit;
    }

    html[data-nsq-theme="dark"] .ejec-machine {
        background: #111b2d;
        border-color: #2d3e55;
    }

    html[data-nsq-theme="dark"] .ejec-machine-head {
        background: #152238;
        border-bottom-color: #2d3e55;
    }

    html[data-nsq-theme="dark"] .ejec-table th {
        background: #101a2b;
        border-bottom-color: #2d3e55;
        color: #9aa9bd;
    }

    html[data-nsq-theme="dark"] .ejec-table td {
        border-bottom-color: #26364d;
        color: #dce7f6;
    }

    html[data-nsq-theme="dark"] .ejec-table tbody tr {
        background: #111b2d;
    }

    html[data-nsq-theme="dark"] .ejec-table tbody tr:hover {
        background: #18263c;
        box-shadow: inset 3px 0 0 rgba(91,166,245,.26);
    }

    html[data-nsq-theme="dark"] .ejec-row-produccion {
        background: #102843 !important;
    }

    html[data-nsq-theme="dark"] .ejec-row-preparacion {
        background: #2b2412 !important;
    }

    html[data-nsq-theme="dark"] .ejec-row-pausado {
        background: #30191d !important;
    }

    html[data-nsq-theme="dark"] .ejec-row-terminado {
        background: #10271d !important;
    }

    html[data-nsq-theme="dark"] .ejec-row-produccion:hover {
        background: #15355a !important;
    }

    html[data-nsq-theme="dark"] .ejec-row-preparacion:hover {
        background: #382f17 !important;
    }

    html[data-nsq-theme="dark"] .ejec-row-pausado:hover {
        background: #3b2025 !important;
    }

    html[data-nsq-theme="dark"] .ejec-row-terminado:hover {
        background: #153423 !important;
    }

    html[data-nsq-theme="dark"] .ejec-status {
        background: #17263c;
        border-color: #354961;
        color: #cbd7e7;
    }

    html[data-nsq-theme="dark"] .estado-produccion {
        background: #102b4c;
        border-color: #28598b;
        color: #9dcaff;
    }

    html[data-nsq-theme="dark"] .estado-preparacion {
        background: #2b2313;
        border-color: #76571e;
        color: #f6cc6d;
    }

    html[data-nsq-theme="dark"] .estado-pausado {
        background: #351719;
        border-color: #7f2b32;
        color: #ff9ca8;
    }

    html[data-nsq-theme="dark"] .estado-terminado {
        background: #102a20;
        border-color: #286446;
        color: #79e3a8;
    }

    html[data-nsq-theme="dark"] .ejec-avance-top {
        color: #aebbd0;
    }

    html[data-nsq-theme="dark"] .ejec-progress {
        background: #26364d;
    }

    html[data-nsq-theme="dark"] .ejec-num.ok {
        color: #79e3a8;
    }

    html[data-nsq-theme="dark"] .ejec-num.sos {
        color: #f6cc6d;
    }

    html[data-nsq-theme="dark"] .ejec-num.scrap {
        color: #ff9ca8;
    }

    html[data-nsq-theme="dark"] .ejec-empty {
        background: #111b2d;
        color: #9aa9bd;
    }

    html[data-nsq-theme="dark"] .ejec-empty-icon {
        background: #17263c;
        border-color: #354961;
        color: #9aa9bd;
    }
    

    /* DARK V2: filas neutras; estatus mediante borde/acento. */
    html[data-nsq-theme="dark"] .ejec-panel,
    html[data-nsq-theme="dark"] .ejec-machine,
    html[data-nsq-theme="dark"] .ejec-empty {
        background: #162133;
        border-color: #2d3d54;
    }

    html[data-nsq-theme="dark"] .ejec-toolbar,
    html[data-nsq-theme="dark"] .ejec-machine-head {
        background: #1a2639;
        border-color: #2d3d54;
    }

    html[data-nsq-theme="dark"] .ejec-table th {
        background: #111b2b;
        border-color: #2d3d54;
    }

    html[data-nsq-theme="dark"] .ejec-table tbody tr,
    html[data-nsq-theme="dark"] .ejec-row-produccion,
    html[data-nsq-theme="dark"] .ejec-row-preparacion,
    html[data-nsq-theme="dark"] .ejec-row-pausado,
    html[data-nsq-theme="dark"] .ejec-row-terminado {
        background: #162133 !important;
    }

    html[data-nsq-theme="dark"] .ejec-row-produccion {
        box-shadow: inset 4px 0 0 #4ea3f7;
    }

    html[data-nsq-theme="dark"] .ejec-row-preparacion {
        box-shadow: inset 4px 0 0 #fbbf24;
    }

    html[data-nsq-theme="dark"] .ejec-row-pausado {
        box-shadow: inset 4px 0 0 #fb7185;
    }

    html[data-nsq-theme="dark"] .ejec-row-terminado {
        box-shadow: inset 4px 0 0 #4ade80;
    }

    html[data-nsq-theme="dark"] .ejec-row-produccion:hover,
    html[data-nsq-theme="dark"] .ejec-row-preparacion:hover,
    html[data-nsq-theme="dark"] .ejec-row-pausado:hover,
    html[data-nsq-theme="dark"] .ejec-row-terminado:hover {
        background: #1b2a40 !important;
    }

    html[data-nsq-theme="dark"] .ejec-status,
    html[data-nsq-theme="dark"] .ejec-visible-count,
    html[data-nsq-theme="dark"] .ejec-tab,
    html[data-nsq-theme="dark"] .ejec-machine-count,
    html[data-nsq-theme="dark"] .ejec-tab-count {
        background: #1c2a40;
        border-color: #38506c;
    }

</style>

<div class="ejec-panel">

    <div class="ejec-toolbar">
        <div class="ejec-toolbar-top">
            <div>
                <h3 class="ejec-title">
                    <i class="bi bi-activity me-1"></i>
                    Ejecuciones de producción
                </h3>

                <div class="ejec-sub">
                    OF tomadas por Producción, mostradas en filas compactas por máquina.
                </div>
            </div>

            <div class="ejec-visible-count">
                <i class="bi bi-list-ul"></i>
                @visibles.Count visible(s)
            </div>
        </div>

        <div class="ejec-tabs">
            <a href="@Url.Action("PanelEjecuciones", "Produccion", new { maquinaId = Model.MaquinaID, busqueda = Model.Busqueda, fechaDesde = Model.FechaDesde, fechaHasta = Model.FechaHasta })"
               class="ejec-tab js-panel-produccion @(Model.EstatusID.HasValue ? "" : "is-active")"
               data-panel="ejecuciones">
                <i class="bi bi-lightning-charge"></i>
                Activas
                <span class="ejec-tab-count">@activas.Count</span>
            </a>

            <a href="@Url.Action("PanelEjecuciones", "Produccion", new { estatusId = ERP.NSQuell.Models.ProduccionEstatus.EnProduccion, maquinaId = Model.MaquinaID, busqueda = Model.Busqueda, fechaDesde = Model.FechaDesde, fechaHasta = Model.FechaHasta })"
               class="ejec-tab js-panel-produccion @(Model.EstatusID == ERP.NSQuell.Models.ProduccionEstatus.EnProduccion ? "is-active" : "")"
               data-panel="ejecuciones">
                <i class="bi bi-play-circle"></i>
                Produciendo
                <span class="ejec-tab-count">@Model.EnProduccion</span>
            </a>

            <a href="@Url.Action("PanelEjecuciones", "Produccion", new { estatusId = ERP.NSQuell.Models.ProduccionEstatus.EnPreparacion, maquinaId = Model.MaquinaID, busqueda = Model.Busqueda, fechaDesde = Model.FechaDesde, fechaHasta = Model.FechaHasta })"
               class="ejec-tab js-panel-produccion @(Model.EstatusID == ERP.NSQuell.Models.ProduccionEstatus.EnPreparacion ? "is-active" : "")"
               data-panel="ejecuciones">
                <i class="bi bi-tools"></i>
                Preparación
                <span class="ejec-tab-count">@Model.EnPreparacion</span>
            </a>

            <a href="@Url.Action("PanelEjecuciones", "Produccion", new { estatusId = ERP.NSQuell.Models.ProduccionEstatus.Pausado, maquinaId = Model.MaquinaID, busqueda = Model.Busqueda, fechaDesde = Model.FechaDesde, fechaHasta = Model.FechaHasta })"
               class="ejec-tab js-panel-produccion @(Model.EstatusID == ERP.NSQuell.Models.ProduccionEstatus.Pausado ? "is-active" : "")"
               data-panel="ejecuciones">
                <i class="bi bi-pause-circle"></i>
                Pausadas
                <span class="ejec-tab-count">@Model.Pausados</span>
            </a>

            <a href="@Url.Action("PanelEjecuciones", "Produccion", new { estatusId = ERP.NSQuell.Models.ProduccionEstatus.Terminado, maquinaId = Model.MaquinaID, busqueda = Model.Busqueda, fechaDesde = Model.FechaDesde, fechaHasta = Model.FechaHasta })"
               class="ejec-tab js-panel-produccion @(mostrarTerminadas ? "is-active" : "")"
               data-panel="ejecuciones">
                <i class="bi bi-check-circle"></i>
                Terminadas
                <span class="ejec-tab-count">@totalTerminadas</span>
            </a>
        </div>
    </div>

    @if (!grupos.Any())
    {
        <div class="ejec-empty">
            <div class="ejec-empty-icon">
                <i class="bi bi-inbox"></i>
            </div>
            <div class="ejec-empty-title">Sin ejecuciones para mostrar</div>
            <div class="ejec-empty-text">
                No hay registros que coincidan con el estado o los filtros seleccionados.
            </div>
        </div>
    }
    else
    {
        foreach (var grupo in grupos)
        {
            <section class="ejec-machine">
                <div class="ejec-machine-head">
                    <div>
                        <span class="ejec-machine-code">
                            <i class="bi bi-hdd-network me-1"></i>
                            @grupo.Key.Codigo
                        </span>

                        @if (!string.IsNullOrWhiteSpace(grupo.Key.Nombre))
                        {
                            <span class="ejec-machine-name">
                                @grupo.Key.Nombre
                            </span>
                        }
                    </div>

                    <div class="ejec-machine-count">
                        <i class="bi bi-gear"></i>
                        @grupo.Count() ejecución(es)
                    </div>
                </div>

                <div class="ejec-table-wrap">
                    <table class="ejec-table">
                        <colgroup>
                            <col style="width:95px;" />
                            <col style="width:210px;" />
                            <col style="width:105px;" />
                            <col style="width:145px;" />
                            <col style="width:125px;" />
                            <col style="width:105px;" />
                            <col style="width:82px;" />
                            <col style="width:72px;" />
                            <col style="width:82px;" />
                            <col style="width:72px;" />
                            <col style="width:135px;" />
                            <col style="width:120px;" />
                            <col style="width:86px;" />
                        </colgroup>

                        <thead>
                            <tr>
                                <th>Programa / OF</th>
                                <th>Parte</th>
                                <th>Molde</th>
                                <th>Operador</th>
                                <th>Inicio real</th>
                                <th>Planeado</th>
                                <th>OK</th>
                                <th>Sosp.</th>
                                <th>Scrap</th>
                                <th>Pend.</th>
                                <th>Avance</th>
                                <th>Estatus</th>
                                <th></th>
                            </tr>
                        </thead>

                        <tbody>
                            @foreach (var item in grupo)
                            {
                                var avance = item.PorcentajeAvance;

                                if (avance < 0)
                                    avance = 0;

                                if (avance > 100)
                                    avance = 100;

                                var avanceCss =
                                avance.ToString(
                                "0.##",
                                System.Globalization.CultureInfo.InvariantCulture);

                                <tr class="@ClaseEstado(item.EstatusID)">
                                    <td>
                                        <span class="ejec-programa">
                                            Programa @item.ProgramaProduccionID
                                        </span>

                                        @if (item.SolicitudProduccionID.HasValue)
                                        {
                                            <span class="ejec-small">
                                                OF @item.SolicitudProduccionID
                                            </span>
                                        }

                                        @if (item.ReleaseID.HasValue)
                                        {
                                            <span class="ejec-small">
                                                Rel. @item.ReleaseID
                                            </span>
                                        }
                                </td>

                                <td title="@Txt(item.DescripcionParte)">
                                    <div class="ejec-parte">
                                        @Txt(item.TituloPrograma)
                                    </div>
                                    <span class="ejec-desc">
                                        @Txt(item.DescripcionParte)
                                    </span>
                                </td>

                                <td>
                                    @(string.IsNullOrWhiteSpace(item.MoldeCodigo)
                                                                ? "-"
                                                                : item.MoldeCodigo)
                        </td>

                                    <td title="@item.TextoOperador">
                                        <div class="ejec-parte">
                                            @item.TextoOperador
                                        </div>
                                    </td>

                                    <td>
                                        @Fecha(item.FechaInicioReal)
                                    </td>

                                    <td class="ejec-num">
                                        @Num(item.CantidadPlaneada)
                                    </td>

                                    <td class="ejec-num ok">
                                        @item.CantidadOKTotal.ToString("N0")
                                    </td>

                                    <td class="ejec-num sos">
                                        @item.CantidadSospechosaTotal.ToString("N0")
                                    </td>

                                    <td class="ejec-num scrap">
                                        @item.CantidadScrapTotal.ToString("N0")
                                    </td>

                                    <td class="ejec-num">
                                        @item.CantidadPendiente.ToString("N0")
                                    </td>

                                    <td>
                                        <div class="ejec-avance">
                                            <div class="ejec-avance-top">
                                                <span>@item.PorcentajeAvance.ToString("0.##")%</span>
                                                <span>@item.CantidadOKTotal.ToString("N0")/@Num(item.CantidadPlaneada)</span>
                                            </div>

                                            <div class="ejec-progress">
                                                <span style="width:@avanceCss%"></span>
                                            </div>
                                        </div>
                                    </td>

                                    <td>
                                        <span class="ejec-status @ClaseBadgeEstado(item.EstatusID)">
                                            <i class="bi @IconoEstado(item.EstatusID)"></i>
                                            @item.EstatusNombre
                                        </span>
                                    </td>

                                    <td class="ejec-action">
                                        <a href="@Url.Action("Detalle", "Produccion", new { id = item.EjecucionProduccionID })"
                                           class="btn btn-sm btn-outline-primary"
                                           title="Abrir ejecución">
                                            <i class="bi bi-eye"></i>
                                            Abrir
                                        </a>
                                    </td>
                                </tr>
                                        }
                        </tbody>
                    </table>
                </div>
            </section>
        }
    }
</div>
